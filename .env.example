# Supabase Environment Configuration Template
# Copy this file to .env and fill in your actual Supabase credentials

# ============================================================================
# SUPABASE DATABASE CONFIGURATION
# ============================================================================
# Get these values from your Supabase project dashboard > Settings > Database

# Supabase PostgreSQL Database Host
# Format: db.<your-project-ref>.supabase.co
SUPABASE_DB_HOST=db.your-project-ref.supabase.co

# Database Port (default: 5432)
SUPABASE_DB_PORT=5432

# Database Name (default: postgres)
SUPABASE_DB_NAME=postgres

# Database User (default: postgres)
SUPABASE_DB_USER=postgres

# Database Password
# Found in: Project Settings > Database > Database password
SUPABASE_DB_PASSWORD=your-database-password-here

# ============================================================================
# SUPABASE API CONFIGURATION
# ============================================================================
# Get these from: Project Settings > API

# Supabase Project URL
# Format: https://<your-project-ref>.supabase.co
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anonymous (Public) API Key
# Safe to use in client-side code, has Row Level Security (RLS) enforced
SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (SECRET - Server-side only!)
# NEVER expose this in client-side code - bypasses RLS
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Supabase JWT Secret
# Used for token validation
SUPABASE_JWT_SECRET=your-jwt-secret-here

# ============================================================================
# PAYMENT ENCRYPTION
# ============================================================================
# Generate a secure base64 key for encryption
# You can generate one with: openssl rand -base64 32
PAYMENT_ENCRYPTION_KEY_BASE64=your-encryption-key-base64-here

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# Server Port
PORT=8080

# Logging Levels (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO
SQL_LOG_LEVEL=WARN
SECURITY_LOG_LEVEL=INFO

# Show SQL queries in console (true/false)
SHOW_SQL=false

# Enable Flyway migrations (true/false)
FLYWAY_ENABLED=true

# ============================================================================
# HOW TO GET YOUR SUPABASE CREDENTIALS
# ============================================================================
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Go to Settings (⚙️) in the sidebar
# 4. Navigate to:
#    - Database settings: for DB_HOST, DB_PASSWORD
#    - API settings: for PROJECT_URL, ANON_KEY, SERVICE_ROLE_KEY
#    - JWT settings: for JWT_SECRET
# ============================================================================

